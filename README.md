# Navichain Story Portal — public deployment

This repository is only the public deployment target for the Navichain Story Portal.
The private source-of-truth repository is not mirrored here.
Website output is published to the `gh-pages` branch and is intentionally configured for non-indexing during the preview phase.
