# Retro Backfill v2 QA — NC-FEM-MINUTER-TILL-GRANSEN

Status: **PARTIAL_BACKFILL**

- No image was rerendered, reconstructed, restyled, repaired, or extracted from a PDF to replace a missing original.
- Provenance and historical QA status are recorded separately.
- `sv-SE` canonical historical visuals with exact bytes directly accessible in the current runtime: **13/14**. `cover_00` is `SOURCE_NOT_EXPORTABLE` for this execution because exact bytes are unavailable and the connected tool surface exposes no platform image-download/file-reference/chat-UI extraction action with which to perform the deterministic recovery permitted by `CHAT-VISUAL-RECOVERY.md`.
- `en-GB` canonical historical visuals with exact bytes directly accessible in the current runtime: **12/14**. `panel_04` and `panel_07` are `SOURCE_NOT_EXPORTABLE` for the same reason.
- Three exact historical carousel PDFs are directly accessible locally: two `sv-SE` and one `en-GB`, each 14 pages. They were not used as replacement sources for missing visual assets.
- `sv-SE` LinkedIn copy is recovered from exact conversation text. `en-GB` social copy was not created.
- The locked/preflight-corrected `sv-SE` Narrative Master is preserved separately from the raw original brief. The `en-GB` story text is a faithful direct translation of that corrected Narrative Master and is not marked Narrative Master.
- Additional failed/divergent render binaries are inventoried as historical files only and are not promoted to canonical assets. Their actual historical QA state is retained where known (`REBUILD_REQUIRED`), otherwise `UNKNOWN_HISTORICAL_QA`.
- The repository contains a binary-backfill assembler workflow, but the connected GitHub action surface available in this chat has no local-file upload parameter for the multi-megabyte local PNG/PDF/ZIP bytes. No false chunk/blob manifest was created.
- The requested ChatGPT → Chrome download → local capture → Nextcloud → hardened router → publisher transport cannot be invoked from the connected tool surface in this chat: no callable Chrome-download or Nextcloud write connector is available. Therefore Nextcloud archive and router/publisher execution remain unverified/not executed.
- GitHub text/metadata writes are performed on an isolated branch and must be read back from `main` after merge before metadata status is reported PASS.

Final release eligibility remains **INCOMPLETE** for both locales; historical backfill does not upgrade visual QA or release status.
